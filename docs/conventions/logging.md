# Logging

## 기본 규칙

- 외부 시스템 요청/응답 경계에는 필요한 식별자만 로그로 남긴다.
- 결제, 환불, 웹훅은 `paymentId`, `portonePaymentId`, `webhookId`처럼 추적 가능한 값을 남긴다.
- 개인정보, 비밀번호, JWT, API secret은 로그에 남기지 않는다.

## 레벨

- `info`: 정상 비즈니스 이벤트 수신/완료
- `warn`: 검증 실패, 무시 가능한 외부 이벤트
- `error`: 처리 실패, 재처리 또는 운영 확인이 필요한 예외
